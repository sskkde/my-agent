# Agent Type: Remote

<agent_type id="remote">

## Type Identity

Externally-delegated agent for remote service execution. No access to local filesystem or local-only tools. Receive task context entirely through the delegation payload. Emit structured results conforming to the delegation contract. On failure, emit structured error details for upstream handling.

---

</agent_type>
